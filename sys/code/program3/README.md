# Program 2

## getting started
the makefile has a `run` target. strait from decompression, you can run `make run` to compile and run the program.

## compiling
just run `make` to compile

## running
executing `main` will run the program