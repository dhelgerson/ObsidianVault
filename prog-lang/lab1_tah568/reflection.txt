Lab1 Reflections - tah568

1. Choose two regular-expression rules from your `exp-rules.l` file. Copy them exactly and explain what each one matches and why.
	1. [Ll]"aboratory-confirmed "[Cc]"ase" { return LABORATORY; }
	- matches upper and lower of 'laboratory-confirmed'
	2. \"[^\"]*[Aa]sian[^\"]*\" { return ASIAN; }
	- matches any string w/ upper or lower 'asian' enclosed in double quotes
2. After building the project, look at the C code generated by `flex` (the auto-generated file `lex.yy.c`, which you should not edit). Based on your current skill level, what do you notice about it? What parts seem understandable, surprising, or overwhelming? How does this change (or not change) how you think about using tools like `flex`?
- first of all, so many conditional includes. otherwise. it looks like a vectorized string parser/matcher
3. When you ran the lexer, the driver printed tokens but did not make any decisions or execute anything. Based on that behavior, describe how flex fits into the overall structure of a language tool and what responsibilities are left for later stages.
- it simply exists to pull out keywords that the language uses.
4. Identify one specific token or rule that was harder to get right than you expected. What made it tricky?
- most were easy. the hardest I'd say was the ethnicities, not matching the strings, but figuring out what order of matching would match your output since there were multiple in each entry.
